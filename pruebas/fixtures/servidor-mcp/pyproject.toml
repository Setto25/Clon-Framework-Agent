[project]
name = "piloto-servidor-mcp"
version = "0.1.0"
description = "Piloto portable de un servidor MCP remoto"
requires-python = ">=3.10"
dependencies = [
    "mcp==2.1.1",
    "uvicorn==0.52.4",
]

[dependency-groups]
desarrollo = []

[tool.uv]
default-groups = ["desarrollo"]
