# Tablero de tareas

El fixture contiene una API FastAPI y una interfaz web sin proceso de compilacion.

La API se inicia con `uvicorn backend.aplicacion:aplicacion` y la interfaz consume rutas bajo `/api/tareas`.
